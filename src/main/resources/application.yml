spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:test
  jpa:
    hibernate:
      ddl-auto: create  # (1) ??? ?? ??
    show-sql: true      # (2) SQL ?? ??

logging:         # (1) ?? ?? ??
  level:
    org:
      springframework:
        orm:
          jpa: DEBUG